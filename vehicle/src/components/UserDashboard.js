import React from 'react';

const UserDashboard = () => {
    const style={
        color: 'blue',
        backgroundColor: 'lightgrey',
        padding: '10px',
        borderRadius: '5px',
        textAlign: 'center'

    };

    return (
        <div style={style}>
            <h1>User Dashboard</h1>
        </div>
    );
};

export default UserDashboard;
